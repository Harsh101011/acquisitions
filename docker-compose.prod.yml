services:
  app:
    build:
      context: .
      target: production
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - PORT=3000
      # DATABASE_URL must be provided at runtime
      - DATABASE_URL=${DATABASE_URL}
    restart: always
